import { TokenExpiringSoonPipe } from 'src/app/shared/pipes/token-expiring-soon.pipe';

describe('TokenExpiringSoonPipe', () => {
  it('create an instance', () => {
    //const pipe = new TokenExpiringSoonPipe();
    //expect(pipe).toBeTruthy();
  });
});
